namespace ChainGang.Resolution
{
//// ReSharper disable InconsistentNaming
    public static class IDependencyScopeExtensions
    {
        //public static IDependencyScope BeginScope(this IDependencyScope scope)
        //{
        //    var tagFactory = scope.GetService<DependencyScopeTagFactory>();
        //    return tagFactory == null
        //               ? BeginScope(scope, null)
        //               : BeginScope(scope, tagFactory(scope));
        //}

        //public static IDependencyScope BeginScope(this IDependencyScope scope, object tag)
        //{
        //    var scopeFactory = scope.GetService<DependencyScopeFactory>();
        //    return scopeFactory == null
        //        ? new DependencyScope() : 
        //}

        //public static IDependencyScope BeginScope(this IDependencyScope scope, IDependencyResolver resolver)
        //{
        //    var scopeFactory = scope.GetService<DependencyScopeFactory>();
        //}
    }
//// ReSharper restore InconsistentNaming
}